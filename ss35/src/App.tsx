import React from "react";

export default function App() {
  return (
    <div>
      {/* hook:móc
        bản chất là những hàm, phương thức giúp cho function component có thể thực hiện dc các tính năng y hệt class component
        +react hook được ra đời ở phiên bản react 16.8
        +trước khi hook ra đời thì function component chỉ đơn giản là tạo ra các UI
        function Header(){
          return <>header<>
        }
        +hook ra đời giúp cho code ngắn gọn dễ dàng sử dụng hơn so với class component
        +dự án bây giờ đa phần sử dụng react-hook
        +sau này đi làm có những dự án vẫn đang dùng class (maintain-chỉnh sửa)
        +khi làm việc với class thì phải nắm liècycle
        +khi làm việc với các hook phải để ý đến đầu vào đầu ra của các phương thức 
        +các hook học
            +useState
            +userEffect
            +useRef
            +useMemo
            +useRenducer
            +useCallback
      */}
    </div>
  );
}
